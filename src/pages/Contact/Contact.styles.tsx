import styled from 'styled-components';
import { HEADER_HEIGHT } from '../../styles/global';

const Wrapper = styled.div`
  position: absolute;
  height: 100%;
  width: 100%;
  overflow-y: scroll;
  padding-top: 0px;
`;

export { Wrapper };
